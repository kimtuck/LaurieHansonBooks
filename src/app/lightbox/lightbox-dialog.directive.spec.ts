import { LightboxDialogDirective } from './lightbox-dialog.directive';

describe('LightboxDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new LightboxDialogDirective();
    expect(directive).toBeTruthy();
  });
});
